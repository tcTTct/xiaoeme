apiVersion: v1
kind: Service
metadata:
  name: vulnerable-ai-agent-demo
  namespace: xiaoeme
spec:
  selector:
    app: vulnerable-ai-agent-demo
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
